import type { Metadata } from "next";
import type { ReactNode } from "react";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Northpoint Systems | Portfolio Landing Page Demo",
  description:
    "A clean, professional business landing page demo built in Next.js with fictional content for portfolio use.",
  applicationName: "Northpoint Systems Demo",
  keywords: [
    "Next.js landing page",
    "portfolio project",
    "fictional SaaS website",
    "AI agency showcase",
    "Tailwind CSS",
    "Framer Motion",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
